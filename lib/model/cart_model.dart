class Item {
  final String name;
  final int productId;
  int count = 0;
  // int quantity;

  Item({
    required this.name,
    required this.productId,
    required this.count,
  });
}
